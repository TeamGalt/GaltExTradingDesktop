//
//  JSUStateModel.swift
//  JSUStockChartDemo
//
//  Created by 苏小超 on 16/7/8.
//  Copyright © 2016年 com.jason.su. All rights reserved.
//

/*
import Foundation

import ObjectMapper

open class JSUNameModel: Mappable {
    open var en  : String?
    open var szh : String?
    
    required public init?(map: Map) {}
    
    open func mapping(map: Map) {
        en  <- map["en"]
        szh <- map["szh"]
    }
}

open class JSUStateModel: Mappable {
    open var code:String?
    open var tick :String?
    open var name :JSUNameModel?
    open var price:Double?
    open var chg:Double?
    open var ratio:Double?
    open var min:Double?
    open var max:Double?
    open var open:Double?
    open var close:Double?
    open var state :String?
    open var scode:Int?
    open var lsdt : String?
    open var tun:Double?
    open var dt:Double?
    open var volume:Double?
    
    public init(){
    }
    
    required public init?(map: Map) {
    }
    
    open func mapping(map: Map) {
        code <- map["code"]
        tick <- map["tick"]
        name <- map["name"]
        price <- map["price"]
        chg <- map["chg"]
        ratio <- map["ratio"]
        min <- map["min"]
        max <- map["max"]
        open <- map["open"]
        close <- map["close"]
        state <- map["state"]
        
        scode <- map["scode"]
        lsdt <- map["lsdt"]
        tun <- map["tun"]
        dt <- map["dt"]
        volume <- map["volume"]
    }
}
*/
